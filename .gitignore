node_modules/
dist/
coverage/
lambda.zip
*.tsbuildinfo

# Terraform
terraform/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

.env
.DS_Store

# plano salvo por `terraform plan -out` (contém valores sensíveis)
tfplan
terraform/tfplan
